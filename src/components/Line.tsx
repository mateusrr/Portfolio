import { Box } from '@chakra-ui/react'

const Line = () => (
  <Box
    display="flex"
    justifyContent="center"
    maxW={{ base: '80%', md: '840px' }}
    borderBottom="2px solid"
    color="blue.500"
    margin="auto"
  />
)

export default Line
